import chunk from 'lodash.chunk';
import sortBy from 'lodash.sortby';
import orderBy from 'lodash.orderby';

export const chunkArray = chunk;

export const sortArrayBy = sortBy;

export const orderArrayBy = orderBy;
